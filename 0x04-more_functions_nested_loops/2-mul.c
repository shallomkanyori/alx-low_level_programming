#include "main.h"

/**
 * mul - multiplies two integers
 * @a: First operand
 * @b: Second operand
 *
 * Return: The result of multiplying the two integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
