#include <stdio.h>

/**
 * main - prints the name of this program
 * @argc: the number of arguments
 * @argv: the arguments
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
